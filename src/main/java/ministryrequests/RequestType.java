package ministryrequests;

/**
 * Represents the type of budget affected by a ministry request.
 */
public enum RequestType {
    /** Taktikos ministry budget. */
    TAKTIKOS,
    /** Investment ministry budget. */
    EPENDYSEIS,
    /** Applies to both ministry budget types. */
    BOTH
}
